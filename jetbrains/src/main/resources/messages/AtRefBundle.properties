settings.title=AtRef

cfg.defaultFormat=Default format
cfg.defaultFormat.comment=Used by the main "AtRef: Copy" action.
cfg.pathStyle=Path style
cfg.pathStyle.comment=workspace = relative to project root; git = relative to nearest .git; absolute = full path; basename = file name only.
cfg.usePosixSeparators=Use POSIX path separators (forward slashes)
cfg.rangeSeparator=Range separator
cfg.rangeSeparator.comment=Separator between start and end line for multi-line selections. Default '-' produces :45-60.
cfg.multiSelectionSeparator=Multi-selection joiner
cfg.multiSelectionSeparator.comment=String used to join references when multiple carets are present. Use \\n for newline.
cfg.notification=Notification
cfg.codeBlockLineNumbers=Include original line numbers in code block
cfg.codeBlockContextLines=Context lines

group.codeBlock=Code Snippet (for "@path:line + Code Snippet")
group.format=Format
group.path=Path

notify.copied=Copied {0}
